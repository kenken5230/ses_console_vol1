# H2 Handoff - 2026-06-27

## Ken's One-Time Checklist

1. Merge the H2 material PRs after review: CI gate workflow, CODEOWNERS, runbook, and Profile update.
2. In GitHub branch protection for `main`, require PRs, required checks, Code Owners review, no force push, no branch deletion, and no bypass.
3. Confirm required checks include `ai-safety-gate`, `typecheck`, `test`, `build`, and the existing Vercel check.
4. Replace or restrict the Codex execution token so it cannot edit repository settings, branch protection, secrets, billing, or admin settings.
5. Run one harmless docs-only test PR to confirm direct push is blocked, checks are required, CODEOWNERS review appears, and Codex cannot disable the protection.

## Not Done By Codex

- Branch protection was not enabled.
- Token permissions were not changed.
- Auto-merge was not enabled.
- Delegated automerge in `AI_PROJECT_PROFILE.md` remains `PENDING / disabled for now`.
- No secret value was generated, read, stored, or output.

## After H2

H2 alone is not enough to enable delegated automerge. H3 standing authorization token handling must also be completed before changing the Profile state.
